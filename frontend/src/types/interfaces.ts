// import React from 'react';
// import { DefaultTheme } from 'styled-components';

export type PlainFunction<T = any> = (params?: T) => any;

export type JSXFunction<T = any> = (params?: T) => JSX.Element;
